feat(viewer): remove legacy mapviewer.py and add Urwid TUI

- Deleted obsolete root-level mapviewer.py and temp runner
- Implemented Urwid-based real-time viewer in kaivosai/viewer.py (with console fallback)
- Added urwid to pyproject dependencies
- Updated README to mention Urwid viewer and install hint

feat(ui): replace REPL with unified Urwid TUI in kaivosai.py

- Removed separate viewer; integrated all functions into main TUI
- kaivosai.py now launches Urwid TUI with live panels: Map, Objects, Clock, Status, Command input
- All REPL commands work in TUI command input (add, remove, move, time, goto, etc.)
- Removed kaivosai-viewer entry point from pyproject.toml
- Removed run_viewer from __init__.py exports
- Updated README with TUI-centric documentation and venv activation instructions
- Falls back to old REPL if Urwid unavailable

fix(db): resolve SQLite database locked errors with WAL mode

- Enabled WAL (Write-Ahead Logging) mode in get_game_conn for better concurrency
- Added 10-second timeout to all DB connections
- Added retry logic in GameClock._set for transient lock errors
- GameClock thread now uses WAL mode to prevent conflicts with main thread

feat(persistence): preserve object IDs and add reset/demo commands

- run_demo() now only adds demo objects if database is empty (no more recreation on startup)
- Added 'reset' command: clears all objects, resets clock, and resets ID counter
- Added 'demo' command: recreates demo objects with explicit IDs 1-5
- Object IDs remain stable across sessions
- persist_object() now respects explicit IDs when provided
- Updated help text and README with new commands
