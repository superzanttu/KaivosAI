Place demo objects at random positions within 30x30 area - Fixed both startup and 'demo' command to use strong randomization with microseconds + os.urandom
Show command with parameters in Status window along with output
Fix demo command: use get() instead of get_object()
Increase map display size limit from 80x40 to 120x60 to accommodate 50x50 map with terrain
Add color support to map display - Robot (cyan), Mine (yellow), Storage (green), Base (magenta), Rock (gray)
Add material system: Mines produce 1/10s (max 10), Storages hold 20, Robots carry 5, Bases consume 1/10s. Production/consumption happens automatically.
Fix backward compatibility: Initialize missing fields for objects loaded from old database
Change default map size to 30x30 to match terrain generation area
Add VERSION constant (0.2.0) to kaivosai package
Remove unused main.py stub file
Add 'version' command and display version in Clock title
