Add rotating spinner to REPL prompt (cycles through |\/-).
Use `prompt_toolkit` for robust interactive prompt and add dependency.
Replace spinner with game clock display in prompt: show hh:mm:ss with blinking colons synced to `GameClock.seconds`.
Expose `load_objects_from_db` and model helpers in package API.
Update `mapviewer.py` to render map and show blinking game clock in header.

Also: fallback prompt now displays the current `hh:mm:ss` (colons blink based on seconds) when `prompt_toolkit` is not available.

Fix: make `GameClock` use monotonic timing to advance `game_seconds` by whole elapsed seconds so the clock ticks at ~1Hz reliably.

Update: mapviewer now prints x/y coordinates (x across top, y at row start) for easier navigation.

Add: `goto` command to start a robot moving to X Y. Robots move 1 cell per game tick, avoid obstacles, recompute path when blocked (BFS pathfinder). Map now attaches to `GameClock` and runs a movement loop.
Fix: movement loop now falls back to wall-clock timing if `GameClock.seconds` is unavailable or not advancing, ensuring robots continue moving at ~1Hz.
Update: include `goto` usage in CLI `help` output.

Fix: mapviewer now prints rows with descending Y so (0,0) is rendered at the bottom-left (more intuitive coordinate layout).

Revert: use top-left origin in mapviewer rendering; restored Y ascending output.

Fix: when moving objects, delete the old DB row before persisting the new position to avoid duplicate rows for the same object.

Action: ran dedupe migration (`python -m kaivosai.migrations`) which created `game.db.bak` and normalized `game_objects` to UNIQUE(x,y).
